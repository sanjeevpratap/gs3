version: "3.10"
services:
  app:
    build:  .
    volumes:
    - .:/app1
    ports:
    - 8000:8000
    # image: app:app1
    container_name: my_first_django_container
    command:  daphne -b 0.0.0.0 -p 8000 django_project.asgi:application
    
  redis:
      image: "redis:alpine"
      environment:
      - REDIS_URL=redis://redis-18739.c212.ap-south-1-1.ec2.cloud.redislabs.com:18739/
 